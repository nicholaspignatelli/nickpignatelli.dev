import styled from "styled-components";
import { Field } from "formik";

const StyledField = styled(Field)`
  border: 0.1rem solid var(--prime-color);
  display: flex;
  align-items: center;
`;

export default StyledField;
