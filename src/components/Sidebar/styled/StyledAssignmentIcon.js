import styled from "styled-components";
import AssignmentIcon from "@mui/icons-material/Assignment";

const StyledAssignmentIcon = styled(AssignmentIcon)`
  margin: var(--px);

  :hover {
    scale: 1.1;
    cursor: pointer;
  }
`;
export default StyledAssignmentIcon;
